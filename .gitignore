*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing material — never commit. The plaintext credentials
# live here only as a temporary working copy until the user moves them
# into a password manager.
RELEASE_CREDENTIALS.txt
*.jks
*.keystore
